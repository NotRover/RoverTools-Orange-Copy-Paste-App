# Agent working notes (see CLAUDE.md → working-notes)
.scratch/

# Private companion repo (Spectrewolf8/orangecp-internal), cloned here as a
# sibling. Never track it from this repo, which is heading public.
orangecp-internal/
